const express = require("express"); // Creamos el router

const route = express.Router(); // corregido a require también si era import antes
// Exportás el router

module.exports = route;
//# sourceMappingURL=estado.routes.js.map
